name: ci-gpu-Apple

on:
  issue_comment:
    types: [created]

jobs:
  macminim1:
    if: |
      github.repository_owner == 'JuliaGPU' &&
      github.event.issue.pull_request &&
      startsWith(github.event.comment.body, 'Test this please')

    runs-on: [self-hosted, ARM64, macOS, macminim1]
    timeout-minutes: 180

    env:
      GH_JOBNAME: ${{matrix.jobname}}

    strategy:
      fail-fast: false
      matrix:
        jobname: [M1-JULIA1_12_3]

    steps:
      # Only trigger CI for certain "actors" (those commenting the PR, not the PR originator)
      - name: Verify actor
        env:
          ACTOR_TOKEN: ${{secrets.TOKENIZER}}${{github.actor}}${{secrets.TOKENIZER}}
          SECRET_ACTORS: ${{secrets.CI_GPU_ACTORS}}
        if: contains(env.SECRET_ACTORS, env.ACTOR_TOKEN)
        id: check
        run: |
          echo "triggered=true" >> $GITHUB_OUTPUT

      # Request repo info, required since issue_comment doesn't point at PR commit, but main
      - name: GitHub API Request
        if: steps.check.outputs.triggered == 'true'
        id: request
        uses: octokit/request-action@v2.x
        with:
          route: ${{github.event.issue.pull_request.url}}
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}

      # Create a separate PR status pointing at GitHub Actions tab URL
      # just like any other third-party service
      - name: Create PR status
        if: steps.check.outputs.triggered == 'true'
        timeout-minutes: 3
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          curl -sS --fail-with-body --retry 3 --retry-delay 5 --retry-all-errors --connect-timeout 10 --max-time 30 \
            -X POST -H "Authorization: Bearer $GH_TOKEN" -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/${{ github.repository }}/statuses/${{ fromJson(steps.request.outputs.data).head.sha }} \
            -d '{"state": "pending", "context": "ci-gpu-Apple-M1 ${{ matrix.jobname }}", "target_url": "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"}'

      - name: Get PR information
        if: steps.check.outputs.triggered == 'true'
        id: pr_data
        run: |
          echo "branch=${{ fromJson(steps.request.outputs.data).head.ref }}" >> $GITHUB_OUTPUT
          echo "repo_name=${{ fromJson(steps.request.outputs.data).head.repo.full_name }}" >> $GITHUB_OUTPUT
          echo "repo_clone_url=${{ fromJson(steps.request.outputs.data).head.repo.clone_url }}" >> $GITHUB_OUTPUT
          echo "repo_ssh_url=${{ fromJson(steps.request.outputs.data).head.repo.ssh_url }}" >> $GITHUB_OUTPUT

      - name: Checkout PR branch
        if: steps.check.outputs.triggered == 'true'
        uses: actions/checkout@v4
        with:
          token: ${{secrets.GITHUB_TOKEN}}
          repository: ${{fromJson(steps.request.outputs.data).head.repo.full_name}}
          ref: ${{steps.pr_data.outputs.branch}}

      - name: Instantiate
        if: steps.check.outputs.triggered == 'true'
        env:
          JULIA_PKG_PRECOMPILE_AUTO: "0"
        run: |
          julia --project -e 'using Pkg; Pkg.instantiate()'
          julia --project -e 'using JACC; JACC.set_backend("Metal")'

      - name: Test
        if: steps.check.outputs.triggered == 'true'
        uses: julia-actions/julia-runtest@v1

      - name: Process Coverage
        if: steps.check.outputs.triggered == 'true'
        uses: julia-actions/julia-processcoverage@v1
        with:
          directories: src,ext/MetalExt

      - name: Upload Coverage
        if: steps.check.outputs.triggered == 'true'
        uses: codecov/codecov-action@v5
        with:
          binary: /Users/wfg/Library/Python/3.9/bin/codecov-cli
          token: ${{ secrets.CODECOV_TOKEN }}
          files: lcov.info
          fail_ci_if_error: true
          override_commit: ${{fromJson(steps.request.outputs.data).head.sha}}
          override_branch: ${{steps.pr_data.outputs.branch}}
          override_pr: ${{github.event.issue.number}}
          flags: metal

      - name: Report PR status
        if: always() && steps.check.outputs.triggered == 'true'
        timeout-minutes: 3
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          curl -sS --fail-with-body --retry 3 --retry-delay 5 --retry-all-errors --connect-timeout 10 --max-time 30 \
            -X POST -H "Authorization: Bearer $GH_TOKEN" -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/${{ github.repository }}/statuses/${{ fromJson(steps.request.outputs.data).head.sha }} \
            -d '{"state": "${{ job.status == 'success' && 'success' || job.status == 'cancelled' && 'error' || 'failure' }}", "description": "job ${{ job.status }}", "context": "ci-gpu-Apple-M1 ${{ matrix.jobname }}", "target_url": "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"}'

  macminim3:
    if: |
      github.repository_owner == 'JuliaGPU' &&
      github.event.issue.pull_request &&
      startsWith(github.event.comment.body, 'Test this please')

    runs-on: [self-hosted, ARM64, macOS, macminim3]
    timeout-minutes: 180

    env:
      GH_JOBNAME: ${{matrix.jobname}}

    strategy:
      fail-fast: false
      matrix:
        jobname: [M3-JULIA1_12_7]

    steps:
      # Only trigger CI for certain "actors" (those commenting the PR, not the PR originator)
      - name: Verify actor
        env:
          ACTOR_TOKEN: ${{secrets.TOKENIZER}}${{github.actor}}${{secrets.TOKENIZER}}
          SECRET_ACTORS: ${{secrets.CI_GPU_ACTORS}}
        if: contains(env.SECRET_ACTORS, env.ACTOR_TOKEN)
        id: check
        run: |
          echo "triggered=true" >> $GITHUB_OUTPUT

      # Request repo info, required since issue_comment doesn't point at PR commit, but main
      - name: GitHub API Request
        if: steps.check.outputs.triggered == 'true'
        id: request
        uses: octokit/request-action@v2.x
        with:
          route: ${{github.event.issue.pull_request.url}}
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}

      # Create a separate PR status pointing at GitHub Actions tab URL
      # just like any other third-party service
      - name: Create PR status
        if: steps.check.outputs.triggered == 'true'
        timeout-minutes: 3
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          curl -sS --fail-with-body --retry 3 --retry-delay 5 --retry-all-errors --connect-timeout 10 --max-time 30 \
            -X POST -H "Authorization: Bearer $GH_TOKEN" -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/${{ github.repository }}/statuses/${{ fromJson(steps.request.outputs.data).head.sha }} \
            -d '{"state": "pending", "context": "ci-gpu-Apple-M3 ${{ matrix.jobname }}", "target_url": "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"}'

      - name: Get PR information
        if: steps.check.outputs.triggered == 'true'
        id: pr_data
        run: |
          echo "branch=${{ fromJson(steps.request.outputs.data).head.ref }}" >> $GITHUB_OUTPUT
          echo "repo_name=${{ fromJson(steps.request.outputs.data).head.repo.full_name }}" >> $GITHUB_OUTPUT
          echo "repo_clone_url=${{ fromJson(steps.request.outputs.data).head.repo.clone_url }}" >> $GITHUB_OUTPUT
          echo "repo_ssh_url=${{ fromJson(steps.request.outputs.data).head.repo.ssh_url }}" >> $GITHUB_OUTPUT

      - name: Checkout PR branch
        if: steps.check.outputs.triggered == 'true'
        uses: actions/checkout@v4
        with:
          token: ${{secrets.GITHUB_TOKEN}}
          repository: ${{fromJson(steps.request.outputs.data).head.repo.full_name}}
          ref: ${{steps.pr_data.outputs.branch}}

      - name: Instantiate
        if: steps.check.outputs.triggered == 'true'
        env:
          JULIA_PKG_PRECOMPILE_AUTO: "0"
        run: |
          julia --project -e 'using Pkg; Pkg.instantiate()'
          julia --project -e 'using JACC; JACC.set_backend("Metal")'

      - name: Test
        if: steps.check.outputs.triggered == 'true'
        uses: julia-actions/julia-runtest@v1

      - name: Process Coverage
        if: steps.check.outputs.triggered == 'true'
        uses: julia-actions/julia-processcoverage@v1
        with:
          directories: src,ext/MetalExt

      - name: Upload Coverage
        if: steps.check.outputs.triggered == 'true'
        uses: codecov/codecov-action@v5
        with:
          binary: /Users/wfg/Library/Python/3.9/bin/codecov-cli
          token: ${{ secrets.CODECOV_TOKEN }}
          files: lcov.info
          fail_ci_if_error: true
          override_commit: ${{fromJson(steps.request.outputs.data).head.sha}}
          override_branch: ${{steps.pr_data.outputs.branch}}
          override_pr: ${{github.event.issue.number}}
          flags: metal

      - name: Report PR status
        if: always() && steps.check.outputs.triggered == 'true'
        timeout-minutes: 3
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          curl -sS --fail-with-body --retry 3 --retry-delay 5 --retry-all-errors --connect-timeout 10 --max-time 30 \
            -X POST -H "Authorization: Bearer $GH_TOKEN" -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/${{ github.repository }}/statuses/${{ fromJson(steps.request.outputs.data).head.sha }} \
            -d '{"state": "${{ job.status == 'success' && 'success' || job.status == 'cancelled' && 'error' || 'failure' }}", "description": "job ${{ job.status }}", "context": "ci-gpu-Apple-M3 ${{ matrix.jobname }}", "target_url": "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"}'
